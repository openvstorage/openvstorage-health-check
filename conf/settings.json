{
    "healthcheck": {
        "library": "/opt/OpenvStorage-healthcheck",
        "logging": {
            "directory": "/var/log/ovs",
            "file": "healthcheck.log"
        },
        "debug_mode": false,
        "max_check_log_size": 500
    }
}